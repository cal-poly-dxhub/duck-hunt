{
  "name": "shared",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "tsc && cp src/frontend-rewrite.js dist/frontend-rewrite.js"
  },
  "devDependencies": {
    "@aws-sdk/lib-dynamodb": "^3.850.0"
  }
}
